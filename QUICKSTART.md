# Quickstart

```sh
npm install          # install dependencies
npm start            # start Expo (development flavor)
npm run ios          # prebuild + run iOS   (dev)
npm run android      # prebuild + run Android (dev)
npm run lint         # lint      | npm run typecheck  — types
npm run test:ci      # tests + coverage
npm run format       # auto-fix Prettier
```
